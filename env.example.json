{
  "TMDB_API_KEY": "your_tmdb_v3_api_key_here",
  "TRAKT_CLIENT_ID": "your_trakt_client_id_here",
  "_comment": "Copy this file to env.json and fill in real values. env.json is gitignored. Trakt client_id is public (used as trakt-api-key header on every request). Trakt client_secret lives only in Firebase Secrets Manager — set via: firebase functions:secrets:set TRAKT_CLIENT_SECRET"
}
